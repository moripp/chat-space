- current_user.groups.each do |group|
  .wrapper__side__contents__box
    = link_to group_messages_path(group) do
      .wrapper__side__contents__box__group
        = group.name
      .wrapper__side__contents__box__text
        テストですよ。aaaaaa